<?php

// A definição do contador
$n = 0;

// Início / Definição da estrutura
while($n < 10){
    
    // Corpo do loop
    echo $n . "<br>";

    // Incremento do contador
    //$n = $n + 1;

    // Incremento do contador
    $n++;
}

echo "Continuando código <br>";


// A definição do contador
$o = 0;

// Início / Definição da estrutura
while($o <= 10){
    
    // Corpo do loop
    echo $o . "<br>";

    // Incremento do contador
    //$o = $o + 1;
    
    // Incremento do contador
    $o += 2;
}

echo "Continuando código <br>";


// A definição do contador
$a = 10;

// Início / Definição da estrutura
while($a > 0){
    
    // Corpo do loop
    echo $a . "<br>";

    // Incremento do contador
    //$a = $a + 1;
    
    // Incremento do contador
    $a--;
}

echo "Continuando código <br>";


// A definição do contador
$p = 10;

// Início / Definição da estrutura
while($p > 0){
    
    // Corpo do loop
    if($p % 2 != 0){
        echo $p . "<br>"; // Exibir números impares do loop

    }

    // Incremento do contador
    //$p = $p + 1;
    
    // Incremento do contador
    $p--;
}

echo "Continuando código <br>";


